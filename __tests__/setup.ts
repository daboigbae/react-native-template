// Jest setup. Add global mocks (MMKV, SecureStore, etc.) here.

// TODO: install @testing-library/jest-native when writing the first component test.
// import '@testing-library/jest-native/extend-expect';
